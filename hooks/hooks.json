{
  "hooks": {
    "user_prompt_submit": [
      {
        "matcher": ".*",
        "command": "hookwise session-check"
      }
    ],
    "PreToolUse": [
      {
        "matcher": ".*",
        "command": "hookwise check"
      }
    ]
  }
}
