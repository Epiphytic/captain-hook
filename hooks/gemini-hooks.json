{
  "hooks": {
    "BeforeAgent": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "name": "session-check",
            "type": "command",
            "command": "hookwise session-check --format gemini",
            "timeout": 10000,
            "description": "Check if the session has a registered hookwise role"
          }
        ]
      }
    ],
    "BeforeTool": [
      {
        "matcher": ".*",
        "hooks": [
          {
            "name": "permission-check",
            "type": "command",
            "command": "hookwise check --format gemini",
            "timeout": 30000,
            "description": "Evaluate tool call against hookwise permission cascade"
          }
        ]
      }
    ]
  }
}
